id = "caldav-channel"
name = "CalDAV Channel"
version = "0.1.0"
kind = "channel"
wasm_path = "plugin.wasm"
description = "CalDAV calendar sync (WASM hooks + sidecar → POST /api/calendar/external/sync)"
permissions = ["network"]

[network]
allowed_url_prefixes = ["https://", "http://127.0.0.1"]
https_only = false
max_requests_per_run = 8
timeout_ms = 30000
